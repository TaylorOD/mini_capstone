json = []

@products.each do |product|
  product_hash = {
    id: product.id,
    name: product.name,
    price: product.price,
    image_url: product.image_url,
    description: product.description,
  }
  json << product_hash
end

return json
